#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: always 0 (success)
 */
int main(void)
{
	char c;

	char d;

	c = 'a';
	d = 'a';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	while
		(d <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
